{
  "name": "rule-extractor",
  "version": "1.0.0",
  "description": "Takes easy list style rules and turns themn into a query string",
  "main": "./dist/index.js",
  "author": "Cory Logan",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "babel-cli": "6",
    "babel-preset-es2015": "6"
  },
  "scripts": {
    "build": "babel src --presets babel-preset-es2015 --out-dir dist"
  }
}
